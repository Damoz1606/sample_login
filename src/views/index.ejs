<%- include('partials/header')%>
    <%- include('partials/nav')%>

        <div class="container-fluid mt-5">
            <div class="container">
                <div class="card">
                    <div class="card-header">
                        <h5>
                            <%= user.local.email %>
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="text-italic">
                            <p>
                                <%= user._id %>
                            </p>
                            <p>
                                <%= user.local.email %>
                            </p>
                        </div>
                    </div>
                    <div class="card-footer">
                        <div class="items-right">
                            <div class="btn-group">
                                <form action="">
                                    <button type="submit" class="btn btn-danger"><span class="fa fa-trash"></span>
                                        Delete</button>
                                    <a href="/index/logout" type="submit" class="btn btn-light"><span
                                            class="fa fa-sign-out-alt"></span>
                                        Log Out</a>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <%- include('partials/footer')%>